<% content_for :boards_menu do %>
  <menu class="O_Menubar">
    <div class="left">
      <% @boards.each do |board| %>
        <%= link_to board.name, board_project_path(board, @project) %>
      <% end %>
    </div>

    <div class="right"></div>
  </menu>
<% end %>

<%= render partial: 'columns/project_column', collection: @board.columns %>
