<p id="notice"><%= notice %></p>

<img src="<%=@image.image %>" alt="" style="width: 200px;">

<p>
  <strong>Name:</strong>
  <%= @image.name %>
</p>

<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
