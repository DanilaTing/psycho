<% if user_signed_in? %>
  <%= react_component("Tasks",
    props: {
      user: @user,
      are_tasks: true,
      project: @project,
      boards: @boards,
      activeMenuTab: @project.name
    },
    prerender: false)
  %>
<% else %>
  <%= render 'sign_in_form' %>
<% end %>
