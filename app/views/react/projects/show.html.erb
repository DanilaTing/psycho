<%= react_component("Project",
  props: {
    project: @project,
    boards: @boards,
    generalBoard: @general_board,
    projectsBoards: @projects_boards,
    inboxColumn: @inbox_column,
    doneColumn: @done_column,
    cardInColumns: @card_in_columns,
    tasks: @tasks
  },
  prerender: false)
%>
