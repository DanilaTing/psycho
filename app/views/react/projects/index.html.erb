<% if user_signed_in? %>
  <%= react_component("Tasks",
    props: {
      user: @user,
      are_projects: @are_projects,
      boards: @boards
    },
    prerender: false)
  %>
<% else %>
  <%= render 'sign_in_form' %>
<% end %>
