<%= react_component("Projects",
  props: {
    board: @board,
    columns: @columns,
    cards: @projects,
    cardInColumns: @card_in_columns,
    inbox_column: @inbox_column,
    done_column: @done_column
  },
  prerender: false)
%>
