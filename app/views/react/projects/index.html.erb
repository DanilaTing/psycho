<% if user_signed_in? %>
  <%= react_component("Tasks",
    props: {
      user: @user,
      are_projects: true,
      boards: @boards,
      activeMenuTab: 'Projects'
    },
    prerender: false)
  %>
<% else %>
  <%= render 'sign_in_form' %>
<% end %>
