<% @projects.each do | project | %>
  <%= project.name %>
  <%= project.type %>
<% end %>

<%= react_component("Projects",
  props: {
    board: @board,
    columns: @columns,
    cards: @projects,
    cardInColumns: @card_in_columns
  },
  prerender: false)
%>
