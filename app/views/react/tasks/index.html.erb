<% if user_signed_in? %>
  <%= react_component("Tasks",
    props: {
      board: @board,
      columns: @columns,
      done: @done,
      inbox: @inbox,
      cards: @tasks,
      cardInColumns: @card_in_columns
    },
    prerender: false)
  %>
<% else %>
  <h1>Hello stranger!</h1>
<% end %>
