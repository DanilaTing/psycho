<% if user_signed_in? %>
  <%= react_component("Tasks",
    props: {
      user: @user,
      are_tasks: @are_tasks,
      boards: @boards
    },
    prerender: false)
  %>
<% else %>
  <%= render 'sign_in_form' %>
<% end %>
