<%= react_component("Tasks",
  props: {
    board: @board,
    columns: @columns,
    done: @done,
    inbox: @inbox,
    cards: @tasks,
    cardInColumns: @card_in_columns
  },
  prerender: false)
%>
