<% if user_signed_in? %>
  <%= react_component("Tasks",
    props: {
      user: @user,
      are_tasks: true,
      boards: @boards,
      activeMenuTab: 'Tasks'
    },
    prerender: false)
  %>
<% else %>
  <%= render 'sign_in_form' %>
<% end %>
